# Raw data

The Madden roster CSV goes at `data-raw/madden-roster.csv`. It's optional. Without it, the build follows the build order's "Starting without the Madden CSV" path and generates a fictional league.
